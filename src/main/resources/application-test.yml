spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/personal_rrhh_test
    username: yafar_dev
    password: rrhh123
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        default_schema: rrhh

  flyway: # ◄ Corregido: de 'flayway' a 'flyway'
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration
    schemas: rrhh
    create-schema: true